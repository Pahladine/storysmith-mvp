import React, { forwardRef } from 'react';

const ChoiceCard = forwardRef(({ onClick, children, emoji }, ref) => (
  <button
    ref={ref}
    onClick={onClick}
    className="w-full text-left p-4 rounded-lg border-2 border-stone-600 hover:border-amber-500 focus:ring-2 focus:ring-amber-500 transition flex items-center space-x-2"
  >
    {emoji && <span className="text-xl">{emoji}</span>}
    <span>{children}</span>
  </button>
));

export default ChoiceCard;